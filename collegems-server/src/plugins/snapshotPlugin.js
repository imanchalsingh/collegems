/**
 * Backward-compatible alias: existing models import snapshotPlugin.
 * Field-level audit trail (#709) now powers snapshots via auditTrailPlugin.
 */
export { default } from "./auditTrailPlugin.js";
